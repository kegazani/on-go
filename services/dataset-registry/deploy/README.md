# Deploy

На шаге `F2` `dataset-registry` используется как offline CLI утилита.

Для production запуска в фазе `J/K` сюда будут добавлены deployment manifests
(например, CronJob/worker orchestration).
