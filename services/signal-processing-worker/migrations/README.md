# Migrations

На шаге `E1` отдельные миграции не требуются.

Worker использует существующие таблицы `ingest.raw_sessions`, `ingest.session_streams`,
`ingest.ingest_artifacts` и `ingest.session_quality_reports`.
