# Tests

Базовые unit-тесты шага `E1`:

1. проверка синхронизации и вычисления `alignment_delta_ms`;
2. проверка очистки и `motion_artifact`/`noisy` флагов;
3. проверка `gap` и `packet_loss` расчетов.
