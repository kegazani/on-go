# Deploy

Для локального запуска шага `E1` подготовлен:

1. `Dockerfile` - контейнер с entrypoint `signal-processing-worker`.

Подключение worker-а в единый compose-стек будет добавлено отдельным инкрементом.
